= render 'manager/navigation'

%p#notice= notice

%div
  %strong Pessoa Jurídica:
  = @legal_person.address

= link_to 'Address', address_path(@legal_person.address), class: 'btn'

%br

= link_to 'Edit', edit_legal_person_path(@legal_person), class: 'btn'
\|
= link_to 'Back', legal_people_path, class: 'btn'
